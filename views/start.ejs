<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Start Page</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            background-image: url('/images/haikei.png'); /* 背景画像のパス */
            background-repeat: no-repeat; /* 繰り返さない */
            background-size: cover; /* 背景画像が画面全体を覆う */
            height: 100vh; /* ビューポートの高さを100%に設定 */
            display: flex; /* フレックスボックスを使用 */
            justify-content: center; /* 水平方向に中央揃え */
            align-items: center; /* 垂直方向に中央揃え */
            flex-direction: column; /* 縦方向に要素を並べる */
        }
        .overlay {
            position: absolute; /* 絶対位置指定 */
            top: 0;
            left: 0;
            width: 100%; /* 幅を100%に設定 */
            height: 100%; /* 高さを100%に設定 */
            background-color: rgba(0, 0, 0, 0.6); /* 黒の半透明を薄く調整 */
            display: flex; /* フレックスボックスを使用 */
            flex-direction: column; /* 縦に並べる */
            justify-content: center; /* 垂直方向に中央揃え */
            align-items: center; /* 水平方向に中央揃え */
            color: white; /* テキストの色を白に設定 */
            text-align: center; /* テキストを中央寄せ */
        }
        #logo {
            max-width: 80%; /* ロゴの最大幅を80%に設定して2倍にする */
            height: auto; /* 高さは自動設定 */
            margin-bottom: 15px; /* ボタンとの間に余白を追加 */
        }
        #nextButton {
            display: block; /* ブロック要素として扱う */
            padding: 10px 20px;
            background-color: #d94a6a; /* 赤とピンクの中間色 */
            color: white;
            border: none;
            border-radius: 10px; /* 角を丸くする */
            cursor: pointer;
            font-size: 16px;
            transition: background-color 0.3s; /* ホバー時の背景色を変更 */
            margin-top: 10px; /* ボタンの上にマージンを追加 */
        }
        #nextButton:hover {
            background-color: #c44161; /* ホバー時の色（赤寄りに変更） */
        }
    </style>
</head>
<body>
    <div class="overlay">
        <img src="/images/logo_fuchi.png" alt="ゲームロゴ" id="logo"> <!-- ロゴを中央に配置 -->
        <button id="nextButton" onclick="location.href='/story'">次へ</button> <!-- storyページに移動 -->
    </div>
</body>
</html>

<!-- views/story.ejs -->
<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Story Page</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            background-image: url('/images/haikei.png'); /* haikei.pngのパスを指定 */
            background-size: cover; /* 背景画像を画面いっぱいに */
            color: white; /* テキストの色を白に設定 */
            font-family: Arial, sans-serif; /* フォントを設定 */
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 100vh; /* 画面の高さを100%に */
        }
        #nextButton {
            margin-top: 20px;
            padding: 10px 20px;
            background-color: rgba(0, 0, 0, 0.7); /* 背景色を少し透明に */
            color: white;
            border: none;
            cursor: pointer;
            font-size: 16px;
        }
    </style>
</head>
<body>
    <div>
        <h1>選挙について</h1>
        <p>とある国で選挙が行われています。</p>
        <p>あなたは選挙管理委員会の一人で開票に携わります。</p>
        <p>一票ごとに有効か不正か仕分けをしてください。</p>
    </div>
    <button id="nextButton" onclick="location.href='/description'">次へ</button> <!-- descriptionページに移動 -->
</body>
</html>